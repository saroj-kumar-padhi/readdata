import 'package:flutter/material.dart';

class PurohitBookingDetails extends StatelessWidget {
  const PurohitBookingDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Purohit Booking Details"),),
      body:Center(child: Text("Nothing Here"),),
    );
  }
}